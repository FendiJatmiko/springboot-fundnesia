version: '3'

services:
  spring-boot:
    image: spring-boot-rest-docker-image
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8000:8080
    volumes:
      - /data/spring-boot-app
